import { Metadata } from 'next';
export const metadata: Metadata = { title: 'Materiały do druku 3D', description: 'Porównaj właściwości i parametry materiałów wykorzystywanych przez KORIX3D.' };
export default function MaterialsLayout({ children }: { children: React.ReactNode }) { return children; }
